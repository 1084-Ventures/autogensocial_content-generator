from typing import Optional, List
from pydantic import BaseModel

class GenerateImageRequest(BaseModel):
    # Add fields as needed from generate-image specs
    pass

class GenerateImageResponse(BaseModel):
    # Add fields as needed from generate-image specs
    pass
